# Binario compilado
/qumran

# Configuración local (contiene credenciales)
.env
.env.*
!.env.example

# Restos de la versión Next.js (por si quedan en despliegues viejos)
node_modules/
.next/
next-env.d.ts
tsconfig.tsbuildinfo
